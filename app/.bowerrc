{
	"directory": "vendor",
	"color": true,
	"ignoredDependencies": [
		"jquery"
	],
	"scripts": {
		"preinstall": "bower --config.cwd=src --config.directory=base-framework --config.scripts.preinstall= install materialize"
	}
}